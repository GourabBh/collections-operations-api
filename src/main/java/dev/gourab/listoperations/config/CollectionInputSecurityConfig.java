package dev.gourab.listoperations.config;

//@Configuration
//@EnableWebSecurity
public class CollectionInputSecurityConfig {
	
	/*
	 * @Bean SecurityFilterChain securityFilterChain(HttpSecurity httpSecurity)
	 * throws Exception { return httpSecurity.csrf(csrf ->
	 * csrf.ignoringRequestMatchers("/h2-console/**")) .authorizeHttpRequests(auth
	 * -> auth.requestMatchers("/h2-console/**").permitAll()
	 * .requestMatchers("/api/search").permitAll() .anyRequest().authenticated())
	 * .headers(headers -> headers.frameOptions().sameOrigin()).build(); }
	 */

}
